# Build outputs
/bin/
/dist/
/diffsmith
/s9-input-budget
*.test
*.out

# Editor / OS
.DS_Store
*.swp
.idea/
.vscode/

# Beads — local-only issue tracker; never committed or pushed
.beads/

# Claude Code per-user permission grants
.claude/*.local.json

# Agent/tooling guidance for this machine only (bd workflow)
AGENTS.md

# Workflow artifacts — local-only by convention (same pattern as docs/)
plans/
spikes/

# Serena MCP server project config — tool-local, not project state
.serena/

# Test artifacts
coverage.out
coverage.html

# Generated at release time by .github/workflows/release.yml — extracts
# the latest CHANGELOG section for the GitHub Release body. Goreleaser
# refuses to release on a dirty working tree, so this must be ignored.
/RELEASE_NOTES.md
