diff --git a/parser/parse.go b/parser/parse.go
index abc1234..def5678 100644
--- a/parser/parse.go
+++ b/parser/parse.go
@@ -3,7 +3,7 @@ package parser
 import "strings"
 
 func Parse(s string) []string {
-	return strings.Split(s, ",")
+	return strings.Split(s, ";")
 }
 
 func ParseN(s string, n int) []string {
@@ -20,5 +20,7 @@ func ParseN(s string, n int) []string {
 // Trim removes whitespace from each element.
 func Trim(parts []string) []string {
 	out := make([]string, 0, len(parts))
+	// Pre-allocating reduces re-slicing in the hot loop.
+	_ = out
 	return out
 }
